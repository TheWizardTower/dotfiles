[Unit]
Description=Emacs Daemon

[Service]
Type=forking
ExecStart=/usr/bin/emacs --daemon
ExecStop=/usr/bin/emacsclient --eval "(kill-emacs)"
Environment=SSH_AUTH_SOCK=/home/rrix/.gnupg/S.gpg-agent.ssh
Restart=always

[Install]
WantedBy=default.target
