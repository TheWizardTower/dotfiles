cd dotfiles       ; docker build -f testDockerfile .               ; cd -;
cd dotfiles/docker; docker build -t thewizardtower/dev_env .       ; cd -;
cd dotfiles/xmonad; docker build -t thewizardtower/xmonad .        ; cd -;
cd dotfiles/puppet; docker build -t thewizardtower/puppet .        ; cd -;
cd dotfiles       ; docker build -t thewizardtower/emacs_config .  ; cd -;
